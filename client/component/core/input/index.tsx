import * as React from 'react'
import { useTheme, makeStyles } from '@material-ui/core/styles';

type Props = {
    name: string
    label: string
}

const InputComponent = (props: Props): null => {
    return null
}

export default InputComponent